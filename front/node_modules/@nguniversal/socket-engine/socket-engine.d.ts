
import { NgModuleFactory } from '@angular/core';
import { StaticProvider } from '@angular/core';
import { Type } from '@angular/core';
import { ɵRenderOptions } from '@nguniversal/common/engine';

export declare interface SocketEngineRenderOptions extends ɵRenderOptions {
    id: number;
}

export declare interface SocketEngineResponse {
    id: number;
    html: string | null;
    error?: Error;
}

declare interface SocketEngineServer {
    close: () => void;
}

export declare function startSocketEngine(moduleOrFactory: Type<{}> | NgModuleFactory<{}>, providers?: StaticProvider[], host?: string, port?: number): Promise<SocketEngineServer>;

export { }
