import { Diagnostics } from './common/diagnostics';
import { TranslationFormat } from './common/util';
export declare const command = "extract";
export declare const describe = "Extract translations from your ivy application";
export declare const builder: {
    s: {
        alias: string;
        required: boolean;
        describe: string;
    };
    f: {
        alias: string;
        required: boolean;
        describe: string;
        choices: string[];
        default: string;
    };
    o: {
        alias: string;
        required: boolean;
        describe: string;
    };
    l: {
        alias: string[];
        required: boolean;
        type: string;
        describe: string;
    };
};
export declare const handler: (options: any) => never;
export interface ExtractTranslationsOptions {
    sourceGlob: string;
    format: TranslationFormat;
    outputPath: string;
    locales?: string[];
    diagnostics: Diagnostics;
}
export declare function extractTranslations({ sourceGlob: source, format, outputPath: output, locales, diagnostics }: ExtractTranslationsOptions): void;
