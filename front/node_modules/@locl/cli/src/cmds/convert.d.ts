import { Diagnostics } from './common/diagnostics';
export declare const command = "convert";
export declare const describe = "Convert translation files from one format to another";
export declare const builder: {
    s: {
        alias: string;
        required: boolean;
        describe: string;
    };
    f: {
        alias: string;
        required: boolean;
        describe: string;
        choices: string[];
        default: string;
    };
    o: {
        alias: string;
        required: boolean;
        describe: string;
    };
};
export declare const handler: (options: any) => never;
declare type TranslationFormat = 'json' | 'xtb' | 'xliff1' | 'xliff2' | 'xlf' | 'xlf2';
export interface ConvertFilesOptions {
    sourceGlob: string;
    format: TranslationFormat;
    outputPath: string;
    diagnostics: Diagnostics;
}
export declare function convertFiles({ sourceGlob: source, format, outputPath: output, diagnostics }: ConvertFilesOptions): void;
export {};
